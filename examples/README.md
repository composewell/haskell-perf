# Simple Programs

hello.hs
console-loop.hs -- console echo loop
delay-loop.hs -- threadDelay in a loop
ffi.hs -- fork one thread + FFI c_sleep

# In memory collection

threadCPUTime.hs -- single threaded stat collection

# Eventlog reporting

traceEventIO.hs -- traceEventIO around c_sleep ffi
traceEventIOWarp.hs -- traceEventIO warp server threads
